{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint",
    "publish": "lerna publish --conventional-commits"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  },
  "dependencies": {
  }
}
